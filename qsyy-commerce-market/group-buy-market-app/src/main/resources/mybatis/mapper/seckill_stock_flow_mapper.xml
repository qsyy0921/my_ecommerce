<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bugstack.infrastructure.dao.ISeckillStockFlowDao">

    <insert id="insertIgnore" parameterType="cn.bugstack.infrastructure.dao.po.SeckillStockFlow">
        insert ignore into seckill_stock_flow(
            flow_no, user_id, activity_id, order_id, out_trade_no,
            stock_bucket, change_type, change_count, stock_before, stock_after,
            biz_event, trace_id, source_message_id, source, message, create_time
        ) values(
            #{flowNo}, #{userId}, #{activityId}, #{orderId}, #{outTradeNo},
            #{stockBucket}, #{changeType}, #{changeCount}, #{stockBefore}, #{stockAfter},
            #{bizEvent}, #{traceId}, #{sourceMessageId}, #{source}, #{message}, now()
        )
    </insert>

    <insert id="insertIgnoreBatch">
        insert ignore into seckill_stock_flow(
            flow_no, user_id, activity_id, order_id, out_trade_no,
            stock_bucket, change_type, change_count, stock_before, stock_after,
            biz_event, trace_id, source_message_id, source, message, create_time
        ) values
        <foreach collection="list" item="item" separator=",">
            (
                #{item.flowNo}, #{item.userId}, #{item.activityId}, #{item.orderId}, #{item.outTradeNo},
                #{item.stockBucket}, #{item.changeType}, #{item.changeCount}, #{item.stockBefore}, #{item.stockAfter},
                #{item.bizEvent}, #{item.traceId}, #{item.sourceMessageId}, #{item.source}, #{item.message}, now()
            )
        </foreach>
    </insert>

</mapper>
